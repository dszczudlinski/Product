spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/product
    username: root
    password: admin
---
logging:
  level:
    org:
      hibernate:
        type: trace